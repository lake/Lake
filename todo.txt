Further cleanup and document preamble.tex.  In particular, document each
included package: what it provides, and any dependencies.

A bug:  if the Bib directory exists, but the latex source contains no \cite
or \nocite, the build fails.

Augmenting the regex like this causes the build to diverge, but there is a
case, I'm forgetting the details, where latex generates the new or term and 
the build fails.
@while egrep -s 'Rerun (LaTeX|to get cross-references right)|LaTeX Warning: There were undefined references.' $(<:aux=log) && [ $$latex_count -gt 0 ] ;

